public class CellStatus{
    //needs to be saved as an enum in vs code
    //need to create a package in vs code titled "sudoku" and write package sudoku at the top
    
    public enum CellStatus{
      GIVEN,
      TO_GUESS,
      CORRECT_GUESS,
      WRONG_GUESS
    }
  }